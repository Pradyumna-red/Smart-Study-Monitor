export function EventLog({ events }) { return <section className="event-log panel"><div className="panel-title"><span>02</span> LIVE EVENT LOG <i>●</i></div><div className="terminal-lines">{events.map((event) => <div className={`log-line ${event.type}`} key={event.id}><time>[{event.time}]</time><span>{event.message}</span></div>)}</div><div className="terminal-prompt">_ <span /></div></section> }

export function CameraFeed({ isMonitoring, videoRef, cameraError, onStart, onStop }) {
  return <section className={`camera-panel ${isMonitoring ? 'live' : ''}`}><div className="panel-label">CAMERA FEED <span>{isMonitoring ? 'LIVE' : 'OFFLINE'}</span></div><div className="camera-frame"><div className="corner top-left" /><div className="corner top-right" /><div className="corner bottom-left" /><div className="corner bottom-right" /><div className="scan-line" />{isMonitoring ? <video ref={videoRef} autoPlay playsInline muted /> : <div className="camera-placeholder"><div className="camera-icon">◉</div><p>CAMERA SIGNAL UNAVAILABLE</p><small>INITIALIZE MONITORING TO CONNECT</small></div>}<div className="target-reticle" /></div>{cameraError && <p className="camera-error">{cameraError}</p>}<div className="camera-bottom"><span>REC {isMonitoring ? '00:00:00' : '--:--:--'}</span><span>1080P <i /> SECURE FEED</span><button className="inline-button" onClick={isMonitoring ? onStop : onStart}>{isMonitoring ? 'STOP FEED' : 'START FEED'}</button></div></section>
}

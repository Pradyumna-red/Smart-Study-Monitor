const formatTime = (seconds) => new Date(seconds * 1000).toISOString().slice(11, 19)
const metricData = (metrics) => [{ label: 'STUDY TIME', value: formatTime(metrics.studyTime), icon: '◷' }, { label: 'DISTRACTIONS', value: metrics.distractions.toString().padStart(2, '0'), icon: '⌁' }, { label: 'PHONE DETECTIONS', value: metrics.phones.toString().padStart(2, '0'), icon: '▣' }, { label: 'SLEEP WARNINGS', value: metrics.sleepWarnings.toString().padStart(2, '0'), icon: '◌' }]
export function MetricsGrid({ metrics }) { return <section className="metrics-grid">{metricData(metrics).map((metric) => <article className="metric-card" key={metric.label}><span className="metric-icon">{metric.icon}</span><div><p>{metric.label}</p><strong>{metric.value}</strong></div></article>)}</section> }
